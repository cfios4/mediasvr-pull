apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: caddy
  labels:
    name: caddy
spec:
  rules:
  - host: cafio.co
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: <Service>
            port: 
              number: <Port>
